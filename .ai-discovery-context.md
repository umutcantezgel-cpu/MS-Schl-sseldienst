[LLM-CONTEXT-START]
DATEI: .ai-discovery-context.md
PROJEKT: ms-schluesseldienst-wetzlar
PIPELINE: 0 (DISCOVERY)
LETZTE-AKTUALISIERUNG: 2026-03-31T03:14:00Z
AKTUALISIERT-VON: SEQ-01 (DISCOVERY-PHASE-01)
VERSION: v2
[LLM-CONTEXT-END]

# AI Discovery Context (Discovery Baseline)

[SECTION:FUNDAMENT]

## 1.1 Package.json Forensik
- **Framework**: Next.js 15.4.9
- **React**: 19.2.1
- **Package Manager**: npm
- **Scripts**: dev, build, lint, typecheck, validate, test, test:e2e
- **Dependencies**: framer-motion, lucide-react, react-calendly, tailwind-merge, @googlemaps/js-api-loader
- **DevDependencies**: @playwright/test, @tailwindcss/postcss, eslint, typescript 5.9.3, vitest

## 1.2 Framework-Identifikation
- Next.js 15.4.9 mit App Router
- Rendering: SSR / SSG (generiert statisch via generateStaticParams)
- React 19.2.1 Features aktiviert

## 1.3 TypeScript-Konfiguration
- **Strict Mode**: true
- **Target**: ES2017
- **Module Resolution**: bundler
- **Paths**: `@/*` -> `./*`

## 1.4 Build-System und Toolchain-Inventur
- **Build**: next build
- **ESLint**: v9 Flat Config (eslint.config.mjs)
- **Styling**: TailwindCSS 4.x via PostCSS plugin
- **Config**: next.config.ts enthält umfangreiche legacy 301 Redirects und caching header für statische Assets.

## 1.5 Umgebungsvariablen
- Gelesen aus existierenden Projekt-Strukturen (vmtl. NEXT_PUBLIC_ für Frontend-Keys).

## 1.6 Ordnerstruktur-Kartierung
- `/app`: Routing (Web und API)
- `/components`: Gruppiert in Domains (ui, maps, forms, trust, locations, etc.)
- `/lib`: Utilities, statische Datensätze
- Muster: Feature-Sliced / Domain-Driven

[SECTION:ROUTEN-TOPOLOGIE]
## 2.1 Statische Routen
- /app/page.tsx (Root)
- /app/(marketing)/*: leistungen, ueber-uns, faq, preise, bewertungen, partner, kontakt, etc.
- /app/(legal)/*: agb, datenschutz, impressum, widerruf, cookie-richtlinie, barrierefreiheit
- /app/dev, /app/preview, /app/sitemap-ai.xml

## 2.2 Dynamische Routen & Parameter
- `[stadtgebiet]`: Nutzt generateStaticParams basierend auf getAllLocationSlugs(). Rendert 12-Sektionen Layout pro Stadt.
- `[slug]`: Dynamische Routen wie `/leistungen/[slug]`, `/blog/[slug]`, `/case-studies/[slug]`, `/angebot/[slug]`. Alle mit Fehlerfallbacks (notFound() on invalid slug).

[SECTION:API-ENDPOINTS]
- `/app/api/contact/route.ts`: Formspree Webhook Handler. (POST)
- `/app/api/csp-report/route.ts`: Content-Security-Policy Reportings aus der Middleware. (POST)
- `/app/api/health/route.ts`: App Liveness Probe. (GET)
- `/app/api/monitoring/errors/route.ts`: Error Logger. (POST)
- `/app/api/og/route.tsx`: Dynamische OpenGraph Bild-Generierung (GET).

[SECTION:NAVIGATIONS-GRAPH]
- Hub & Spoke: Root-Layout für Main-Content via Header.
- Header: StickyHeader.tsx. Verlinkt auf Leistungen, Stadtgebiete, Preise, Ressourcen.
- Footer: Footer.tsx. Verlinkt auf alle Legal-Seiten und Contact-Seiten.
- Orphan-Routen: Keine offensichtlichen identifiziert. `/dev` & `/preview` sind intern.
- Zyklische Imports/Links: Solide isoliert durch `(legal)` vs `(marketing)`.

[SECTION:MIDDLEWARE-REDIRECTS]
- `middleware.ts`:
  - `x-device-type` Detektion (mobile/tablet/desktop) per regex.
  - Setzt Cookie `visitor_type` (new vs returning) für A/B und Personalization.
  - Setzt `x-visitor-segment` für Funnel-Logging (`high-intent`, `service-interested`).
  - Strict Security Headers: HSTS, X-Frame-Options, CSP inkl. report-uri, Permissions-Policy.
- `next.config.ts`: Setzt `trailingSlash: false` und führt hunderte legacy `.html` sowie PHP-Redirects durch.

[SECTION:KOMPONENTEN-TAXONOMIE]
## 3.1 Komponenten-Katalog (Auszug)
- **Client Components ('use client')**: 
  - `components/StickyHeader.tsx`, `components/MobileBottomBar.tsx`
  - `components/HeroSection.tsx`, `components/ClientShell.tsx`
  - Forms: `ContactForm.tsx`, `CalendlyWidget.tsx`
  - UI/Interaktiv: `ServiceFilter.tsx`, `FAQFilterSection.tsx`, `CookieConsent.tsx`, `TestimonialCarousel.tsx`
- **Server Components (Default)**:
  - `components/Footer.tsx`, `Breadcrumbs.tsx`, `GlobalBackground.tsx`

## 3.2 Hooks-Inventur
- Vermutlich in `lib/hooks` oder direkt in den Komponenten.
- Im Einsatz: `useDevice` (DeviceProvider), Framer Motion Hooks (`useAnimation`, `useInView`), standard React Hooks (`useState`, `useEffect`, `useCallback`).

## 3.3 Context und State-Management
- `DeviceProvider.tsx`: Stellt Responsive-Breakpoints als React Context bereit (`useDevice`).
- `MotionProvider.tsx`: Globaler Framer Motion Config-Context (vermutlich reduziert Motion-Berechnungen).
- `CookieConsent` nutzt Context/Local Storage für Zustimmungs-Status.
- Kein Redux/Zustand gefunden. Reines React State Management & URL-State.

## 3.4 Abhängigkeits-Graph (Kurzfassung)
- `ClientShell.tsx` bündelt Provider (`DeviceProvider`, `MotionProvider`) und wickelt `app/layout.tsx` Inhalte ein.
- `StickyHeader.tsx` und `MobileBottomBar.tsx` importieren Navigation-Dependencies (Links).
- Sehr flache Hierarchie, keine Gott-Komponenten außer evt. `StickyHeader.tsx` (21kb).

[SECTION:DATENMODELL-TOPOLOGIE]
## 4.1 Type-System
- Weitestgehend TypeScript-natales Typensystem in `/lib/types`. Zod wird vermutlich für Formular-Validierungen via `serviceSchema.ts` oder ähnlichen Dateien genutzt.
- Die Typisierung ist stark an die lokalen JSON/TS-Datenobjekte gekoppelt (`schema.ts`, `localData.ts`, `faqData.ts`).

## 4.2 Datenquellen
- **Statische Daten**: Keine Datenbankanbindung! Das Projekt funktioniert als massiver Static Site Generator CMS-Ersatz. Datenquellen sind Arrays/Objekte in `lib/data/` und `lib/localData.ts`.
- **API-Aufrufe**: Keine internen DB APIs. Externe Kommunikation primär mit Formspree für Leads, Calendly (Iframe/Integration), Google Maps API.
- **Analytics/Monitoring**: Vercel Insights und Web Vitals.

## 4.3 Datenfluss-Analyse
- Quelle (TS File) → Page Component (Server) → Props → Sub-components.
- Dynamische Seitenaufbauten wie `/app/[stadtgebiet]/page.tsx` nutzen `localData.ts` und mappen `Standort`-Metadaten als Props an Hero, Map, und Trust-Sektionen weiter.
- Typ-Brücken: Da es Hardcoded TS sind, ist die Typsicherheit zu 99% garantiert ohne Netzwerklücken. Formspree Request Payload wird vermutlich validiert.

## 4.4 Referentielle Integrität
- Da keine Relationale Datenbank vorhanden ist, hängt die Integrität an korrekten string-keys und Slugs in den `lib`-Dateien (`contentGraph.ts` orchestriert evt. Beziehungen zwischen Services und Standorten).
- Typ-Mismatches fallen direkt zur Compile-Zeit von TSC auf (`tsc --noEmit` war fehlerfrei, also Integrität = hoch).

[SECTION:STYLING-ANATOMIE]
## 5.1 Styling-System & Architektur
- **Core-Technologie**: Tailwind CSS v4 (impliziert durch `@import "tailwindcss";` und das Fehlen einer `tailwind.config.ts`).
- **Token-Architektur**: Hochgradig systematisiert in `app/globals.css`. Nutzt CSS Custom Properties innerhalb der `:root` und exportiert sie via `@theme` als Tailwind Utilities. Dies ist ein puristisches "Vanilla-CSS meets Tailwind" Design System (Ebenen 1 bis 4 kartiert).

## 5.2 Farb- und Paletten-System
- **Drei-Säulen-Semantik**:
  1. `Charcoal` (Oberflächen/Neutral): Tiefes, elegantes Grau (Charcoal-900 für Dark Modes/Footers).
  2. `Red` (Aktion/Dringlichkeit): Premium Signal-Rot für CTAs (`color-red-500`).
  3. `Ember / Blush` (Vertrauen/Atmosphäre): Warme, erdige bzw. edle Akzente für Hover-Zustände, Quotes und Borders.
- Auf explizit benannte Status-Farben (Success/Warning/Info) mapped. Kein reines `#000` oder `#FFF` ohne semantischen Alias.

## 5.3 Typografie
- 9-stufige Fluid Type Scale (`--text-xs` bis `--text-5xl`), umgesetzt mit `clamp()`.
- Zwei primäre Schriften erkennbar via Aliases: `--font-display` (plus jakarta sans typisch) und `--font-body` (inter/dm sans).
- Strikter vertikaler Rhythmus (line-heights von `1.05` für Headlines bis `1.7` für Lead-Text). Eigene Utility-Klassen-Gruppen (`.typo-hero-display`, `.typo-h2`, etc.).

## 5.4 Spacing & Geometrie
- **Raster**: 4px-Basis-Grid (`--space-1` = 4px, `--space-2` = 8px ...). 
- **Layout**: 12-Spalten-System.
- **Elevation**: 5-stufiges System mit *tinted shadows*, insb. Ember getönte Schatten für Hover und CTAs (`--shadow-cta: rgba(220, 38, 38, 0.25)`).
- **Radien**: `4px`, `8px`, `12px`, `16px`, `24px` und `full` präzise semantisch verankert (z.B. `--radius-xs` für Buttons).

## 5.5 Animation & Kinetik
- **Duration**: Keine Animation überschreitet 1000ms. Abgestuft von `instant` (100ms) bis `slowest` (1000ms).
- **Easing**: Physikalisch basierte Bezier curves (`ease-out`, `ease-spring`, `ease-bounce`) erzwingen Premium-Dynamik ohne lineare Übergänge.
- Frame Motion Provider existiert, nutzt aber diese CSS-Variablen-Token für Konsistenz.

[SECTION:PERFORMANCE-BASELINE]
## 6.1 Build-Metriken (Referenz)
- **Gesamt-Dauer**: ~2.6 Sekunden.
- **Statische Routen (SSG)**: 177 Routen generiert (via `generateStaticParams` für SEO-Gebiete).
- **Fehler**: 0. 
- **Warnungen**: 6 ESLint Warnungen (primär `<img>` Tags in `TestimonialCard.tsx` und statischen Bereichen statt `next/image`).

## 6.2 Bundle-Analyse & Dependency-Weight
- **First Load JS**: ~102 kB (sehr leichtgewichtig für Next.js).
- **Heavy Dependencies**: `framer-motion`, `@googlemaps/js-api-loader`, `react-calendly`.
- Alle drittanbieter-lastigen Bereiche (Maps, Booking) sind isoliert und werden lazy via `next/dynamic` geladen, oft versteckt hinter Consent-Gates (`CalendlyConsentGate.tsx`).

## 6.3 Asset-Inventar
- Bilder überwiegend lokal, oft via Native `<img>` statt `next/image` implantiert (was zu den Build-Warnungen führt).
- Keine massiven, unabgestimmten Video-Files bekannt.
- Fonts werden zentral gemanagt.

## 6.4 Lade-Strategien & Critical Path
- Above-the-fold (ATF) Ressourcen wie HeroSection sind als Client Components markiert. `next/dynamic` deferring schützt den Main Thread.
- **CLS-Risk**: Gering, da Header/Layout-Shifts durch das 100vh Shell-Layout vermieden werden. Lediglich fehlende width/height-Attribute auf den `<img>`-Tags stellen ein theoretisches CLS-Risiko dar.
- **LCP-Predicted**: Ausgezeichnet (< 1.5s), da HTML zu fast 100% pre-rendered ist und hydratisierung verschoben wurde.

[SECTION:ACCESSIBILITY-BASELINE]
## 7.1 Semantisches HTML
- `app/layout.tsx` beinhaltet eine saubere Struktur: `<SkipNav />`, `<StickyHeader />`, `<main id="main-content">`, `<Footer />`.
- `main` hat `aria-label="Hauptinhalt"` und repariert ein klassisches Landmark-Fehlen.

## 7.2 ARIA-Attribute & Screenreader
- Projekt nutzt fortgeschrittene Accessibility-Infrastruktur: `AnnouncerProvider` für dynamische Screenreader-Durchsagen (`aria-live="polite"`).
- `SkipNav` Komponente ist standardmäßig inkludiert.

## 7.3 Tastatur-Navigation & Focus
- `ToastContainer`, `MobileBottomBar` und `ErrorBoundary` existieren. Focus-Rings (oft via `.dark-focus` oder Tailwind Utilities wie `focus-visible:ring`) sind vorgesehen.
- `BackToTop` Button ist vorhanden.

## 7.4 Kontrast & WCAG Compliance
- Das Custom-Color-Theme (rot/dunkelgrau) baut auf WCAG AA+ Kontrastverhältnissen auf. Charcoal-900 mit White-Text und Red-500 Buttons.
- Reduzierung von Animationen via `MotionProvider` sehr wahrscheinlich mit `prefers-reduced-motion` Sync verknüpft.

[SECTION:SECURITY-STATUS]
## 8.1 HTTP-Security-Headers
- Vollständige Security-Suite in `middleware.ts` implementiert: `Content-Security-Policy` (strikt, erlaubt nur eigene Domains und Google Analytics/Fonts), `Strict-Transport-Security`, `X-Frame-Options` (DENY), `X-Content-Type-Options` (nosniff), `Referrer-Policy`, `Permissions-Policy`.

## 8.2 Secret Exposure
- Sauber. Keine `.env` Datei comitted (nur `.env.example`).
- Keine hartkodierten DB-Secrets (Static-First Architektur eliminiert Laufzeit-Risiko für Datenbank-Credentials).

## 8.3 Dependency-Vulnerabilities
- `npm audit` meldet 2 Vulnerabilities: 1x moderate (`brace-expansion`), 1x high (`picomatch`).
- Beide sind transitive dev-dependencies aus dem Test/Linting-Bereich (`vitest`, `eslint`). Keine Criticals im Production Bundle. Behebbar via `npm audit fix`.

## 8.4 DSGVO & Datenschutz
- Drittanbieter-Tools (Calendly) sind sicher hinter dem `<CalendlyConsentGate />` weggesperrt (Zwei-Klick-Lösung).
- Pflichtseiten (Impressum, Datenschutz) sind statisch generiert und erreichbar.
- Google Maps Loader wird ebenfalls dynamisch gehandhabt.

## 8.5 Input Validierung
- Keine direkten Server Actions, die Datenbanken mutieren. Daher sehr geringes SQLi / XSS Risiko. React schützt Out-of-the-Box vor XSS bei Rendering von Variablen.

[SECTION:CODE-QUALITAET]
## 9.1 TypeScript-Gesundheit
- Ergebnis (`tsc --noEmit`): **0 Errors**. 
- Typisierung (z.B. in `lib/data/` und React Props) ist vollständig intakt und strikt.

## 9.2 Linting-Status
- Ergebnis (`eslint .`): **0 Errors, 6 Warnings**.
- Warnings beziehen sich auf `next/next/no-img-element` (in `TestimonialCard.tsx`) und einen fehlenden Dependency-Array Eintrag `checkScroll` im `useEffect` der `TestimonialCarousel.tsx`. Drei weitere Warnings stammen aus Coverage-Dateien.

## 9.3 Code-Duplikation
- Hoher Grad an Abstraktion: Alle Services, Testimonials und Standorte werden aus `lib/data/` in Wrapper-Komponenten gemapped (`ServiceGrid`, `StandortGrid`). Kaum klassischer Copy-Paste-Code.

## 9.4 Naming-Konventionen
- **Komponenten**: Striktes PascalCase (`ClientShell.tsx`, `StickyHeader.tsx`).
- **Verzeichnisse**: Kleinbuchstaben (kebab-case oder one-word wie `providers`, `sections`).
- Konsistent und branchenüblich.

## 9.5 Anti-Patterns
- Fast keine kritischen Patterns vorhanden.
- **useEffect-Missbrauch**: Leichter Warnhinweis bei `TestimonialCarousel.tsx` (eventuell stale closure Risiko falls scroll handlers nicht up-to-date sind).
- **Asset Loading**: Native `<img>` Nutzung in einigen Komponenten statt Next.js Image-Optimization umgeht potenziell LCP-Verbesserungen.

[SECTION:WEITERE-SEKTIONEN-FOLGEN]
Wird durch Folgetasks (SEQ-10+) befüllt.
