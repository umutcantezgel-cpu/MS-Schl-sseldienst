# AI DESIGN CONTEXT

## STATUS
**INITIALISIERT DURCH**: DIRECTOR OF DISCOVERY (Pipeline 0, SEQ-10)
**ZIEL**: Single Source of Truth für den Director of Design. Enthält Styling-Anatomie und A11y.

[SECTION:STYLING-ANATOMIE]
- **Tailwind V4**: Das Projekt verwendet Tailwind CSS V4, basierend auf CSS-Variablen in `app/globals.css`.
- Keine `tailwind.config.ts`, Configuration geschieht nativ via `@theme` in CSS.
- **Farben**: Semantische Mappings (`bg-background`, `text-primary`, `bg-brand-red`).
- **Headless UI**: Keine monolythischen Component Libraries (wie Shadcn). Eigene, hochgradig optimierte Tailwind Components (`Button.tsx`, `Badge.tsx`).
- **Motion**: `framer-motion` für Reveal-Animationen, Hero Entrances (`FramerMotionAdapter.tsx`).

[SECTION:ACCESSIBILITY-BASELINE]
- ARIA-Controls, `aria-expanded` in komplexen Dropdowns wie `StickyHeader.tsx` perfekt umgesetzt.
- **Tastatur-Fokus**: Bewusste Einbindung von Focus-Traps in Modal-Overlays (`handleOverlayKeyDown` mit Shift+Tab Interception).
- **A11y-Defekt (P2)**: Globaler Wrapper im `layout.tsx` benötigt `<main>` Definition für "Skip to Content".

[/SECTION]
