# AI QA CONTEXT

## STATUS
**INITIALISIERT DURCH**: DIRECTOR OF DISCOVERY (Pipeline 0, SEQ-10)
**ZIEL**: Single Source of Truth für den Director of Quality/Security. Enthält Coverage & Metriken.

[SECTION:SECURITY]
- Strenge `Content-Security-Policy` und `Permissions-Policy`. HTTP `Strict-Transport-Security` preload vorhanden. Keine Secret-Leaks.
- CMS Input Sanitization via `sanitizeHTML()` verifiziert.
- `dangerouslySetInnerHTML` wird legal für JSON-LD (Rich Snippets) in Header-Bereichen genutzt.

[SECTION:PRE-EXISTING-DEFECTS]
- 3 Warnings im Linter bzgl. `eslint-disable` in ignorierten Coverage Files.
- Kein Runtime/Build Error vorhanden. Der Code ist extrem sauber (Grade A+).

[/SECTION]
